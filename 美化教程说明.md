# Windows Terminal 美化配置说明

### 1. 将 CMD 默认终端改为 Windows Terminal （可选）

修改当前用户注册表：

- 路径：`HKCU\Console\%%Startup`
- `DelegationConsole`：`{2EACA947-7F5F-4CFA-BA87-8F7FBEEFBE69}`
- `DelegationTerminal`：`{2EACA947-7F5F-4CFA-BA87-8F7FBEEFBE69}`

这两个值是 Windows Terminal 的标识符，因此从运行窗口、资源管理器地址栏等位置启动 `cmd` 时，会在 Windows Terminal 中打开。

### 2. 开启亚克力效果

修改了 Windows Terminal 配置文件：

`%LOCALAPPDATA%\Packages\Microsoft.WindowsTerminal_8wekyb3d8bbwe\LocalState\settings.json`

主要配置如下：

```json
{
  "useAcrylic": true,
  "acrylicOpacity": 0.60,
  "opacity": 82
}
```

- `useAcrylic: true`：开启亚克力背景效果。
- `acrylicOpacity: 0.60`：降低背景层不透明度，让桌面背景的模糊效果更明显。
- `opacity: 82`：设置窗口整体透明度。

### 3. 字体设置

```json
{
  "fontSize": 15,
  "fontFace": "Cascadia Mono",
  "fontWeight": "semi-bold"
}
```

字体使用 `Cascadia Mono`，字号为 15，并使用半粗体提高可读性。

### 4. 淡紫色主题

当前 `cmd` 使用淡紫色亚克力配色：

```json
{
  "background": "#7F6D8D",
  "foreground": "#FFFFFF",
  "cursorColor": "#FFFFFF",
  "selectionBackground": "#B9A6C5"
}
```

- 背景：柔和淡紫色。
- 文字：纯白色，保证对比度。
- 光标：白色。
- 选中区域：浅紫色。

## 配置生效方式

新打开的 Windows Terminal 窗口或新建的 `cmd` 标签页会使用这些设置。若已有标签页没有立即更新，请关闭该标签页后重新打开。

## 配置备份

每次修改前都会在 Windows Terminal 的 `LocalState` 文件夹中生成一个带时间戳的 `.bak` 配置备份文件，可用于恢复之前的设置。


